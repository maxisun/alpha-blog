<% if obj.errors.any? %>
  <div class="alert alert-danger" role="alert">
    <h2 class="alert-heading">
      <%= pluralize(obj.errors.count, "error") %> prevented your article from being saved 
    </h2>
  <hr>
    <ul class="mb-0">
      <% obj.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>