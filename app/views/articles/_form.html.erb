  <% if @article.errors.any? %>
    <h2><%= pluralize(@article.errors.count, "error") %> prevented your article from being created </h2>
    <ul>
      <% @article.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
        <% end %>
    </ul>
  <% end %>

<!--

  <%= form_with(model: @article, local: true) do |f |%>
    <p>
      <%= f.label :title %> <br>
      <%= f.text_field :title %>
    </p> 

    <p>
      <%= f.label :description %> <br>
      <%= f.text_area :description %>
    </p> 

    <p>
      <%= f.submit %>
    </p> 

  <% end %>

  <p>
    <%= link_to "Back to articles listing", articles_path %>
  </p>
-->
  <%= form_with(model: @article, local: true) do | f |%>
    <div class="form-group row">
      <%= f.label :title, class: "col-sm-2 col-form-label"%>
      <div class="col-sm-8">
        <%= f.text_field :title, class: "form-control", placeholder: "Title of article", autofocus: true %>
      </div>
    </div> 

    <div class="form-group row">
      <%= f.label :description, class: "col-sm-2 col-form-label" %>
      <div class="col-sm-8">
        <%= f.text_area :description, class: "form-control", placeholder: "Description of article" %>
      </div>
    </div> 

    <div class="form-group row">
      <div class="col-sm-10">
        <%= f.submit class: 'btn btn-primary '%>
      </div>
    </div> 

    <%= link_to "cancel and return", articles_path%>

  <% end %>